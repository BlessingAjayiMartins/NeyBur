import React, { useState } from 'react'
import {
  StyleSheet
} from 'react-native'
import { Layout, Text, Button } from '@ui-kitten/components'

const addPost = () => {
  const [state, setState] = useState({text: '', img: null})
}